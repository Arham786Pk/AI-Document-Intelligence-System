# AI French Invoice Extraction — Milestones 1 + 2
# Install: pip install -r requirements.txt
# Note: Tesseract OCR + the fra (French) language pack must be installed
#       separately at the OS level. See README.md > Task 5.

# Document I/O
pymupdf>=1.24
pdf2image>=1.17
pillow>=10.0

# Preprocessing
opencv-python>=4.9
numpy>=1.26

# OCR
pytesseract>=0.3.10
paddleocr>=2.7
paddlepaddle>=2.6

# Rule-based extraction (accent-tolerant regex)
regex>=2024.5
python-dateutil>=2.9

# Evaluation (CER/WER, P/R/F1)
jiwer>=3.0
pandas>=2.2

# Spreadsheet I/O for ground truth + metrics
openpyxl>=3.1

# Utilities
tqdm>=4.66
requests>=2.32

# Milestone 2 — synthetic data generation
faker>=25.0
reportlab>=4.0

# Milestone 2 — annotation (Task 03)
label-studio>=1.11
label-studio-sdk>=1.0
