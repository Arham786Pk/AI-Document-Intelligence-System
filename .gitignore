# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.python-version

# IDE
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Claude Code session artifacts
.claude/

# (Dataset is synthetic / non-confidential — committed to repo for reproducibility)

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Misc
~$*.docx
~$*.xlsx

# Real-PII invoices held locally for review — never push to public repo (M2 Task 02)
quarantine/pii_review/

# Quarantined non-invoices / low-quality PDFs — preserved locally, excluded from repo
quarantine/

# Regenerable preprocessing cache (rebuilt by `python -m src.preprocessor`)
data/processed/

# Scratch / temp render dirs
scripts/_*
